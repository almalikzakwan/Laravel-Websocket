{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "laravel:serve": "php artisan serve --host=0.0.0.0 --port=8000",
        "laravel:reverb": "php artisan reverb:start",
        "laravel:worker": "php artisan queue:work --sleep=3 --tries=3"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.4"
    },
    "dependencies": {
        "laravel-echo": "^2.2.0",
        "pusher-js": "^8.4.0"
    }
}
